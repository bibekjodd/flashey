import { Poppins } from 'next/font/google';
export const poppins = Poppins({
  weight: ['300', '400', '500', '600', '700', '900'],
  subsets: ['latin']
});

import { Concert_One } from 'next/font/google';
export const concertOne = Concert_One({ weight: ['400'], subsets: ['latin'] });
